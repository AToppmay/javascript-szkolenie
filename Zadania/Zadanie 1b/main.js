import { wyswietl } from '../../shared/helpers.js';

// Tutaj są podane teksty do sprawdzenia
const tekst1 = 'jAn KoWaLsKi';
const tekst2 = 'LOREM ipsum';

// Dodaj logikę do tej funkcji
function odwrocLitery(tekst) {
    let odpowiedz = '';
    // tutaj dodaj pętlę for
    return odpowiedz
}


wyswietl(odwrocLitery(tekst1));
wyswietl(odwrocLitery(tekst2));